# bash/.pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: shfmt
        name: shfmt (write)
        language: system
        entry: shfmt -w -i 2 -ci
        files: '^(xwrap|.*\.sh)$'
        require_serial: true
